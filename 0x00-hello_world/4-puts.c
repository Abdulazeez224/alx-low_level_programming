#include <stdio.h>

/**
 * main - Entry point
 * @void: Specify no argument
 *
 * Description: The return value of main is to indicate how the program exited
 * Return: Always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
